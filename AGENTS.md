# AGENTS.md

## 工资表应用架构

- 依赖方向固定为 `excel → rules → ui`，底层不得反向引用上层。
- Excel/OOXML 能力只放 `app/excel/`；业务规则只放 `app/rules/`；页面编排只放
  `app/ui/`。
- 不得在 UI 或导出代码中臆造工资公式、社保上下限、取整或其他未确认业务规则。
- 新规则必须是独立模块，导出纯函数和规则元数据，并同步无 PII 测试、产品规格和验收
  清单。
- 规则模块不超过 400 行，Excel/UI 模块不超过 500 行，入口文件不超过 100 行。
- 修改依赖或拆分模块后必须运行 `npm run check:architecture`。

## 数据与隐私

- 原始工资表和部门附件全程只读，不得复制进 Git、测试夹具或发布包。
- 测试只使用合成人员编号、合成证件标识和虚构数值。
- 不得提交姓名、身份证号、银行卡号、手机号、工资数值、导出物、截图或真实回放。
- 任何外部来源都必须先预览逐项匹配和错误，再由用户确认应用。
- 无法安全匹配或同步时必须停止，不得静默填 0 或猜规则。

## 月度生成入口

- 页面只接收上月完整工资表，目标固定为下一个自然月。
- 当前月份优先由主工资表标题证明，文件名只作辅助。
- 上月工资表提供结构、样式、内部公式和历史缓存，不应转成变动提案。
- 人员与工资变动只通过文字或表格入口接收，表格格式由内容自动判断。
- 目标为一月时必须读取上一年度 12 个月。
- 导出前必须解析已确认来源并移除全部外链；结果始终标记为待复核。

## 维护入口

开始修改前完整阅读 `CODEX.md`、
`requirements/PRODUCT_SPEC.md` 和 `requirements/ACCEPTANCE.md`。完成后运行
架构检查、端到端测试和两类发布构建，并检查 Git 暂存内容。
