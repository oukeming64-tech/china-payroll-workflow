# 更新记录

## v1.2.0 — 2026-07-29

- 目标月份附件不再限制数量，一次选择的全部有效文件都会读取和综合核对；
- 支持同一工作表中的转正、转岗、调薪等多个独立表格区段；
- 有身份证的人员变动严格按身份证匹配，禁止未命中时静默改用姓名；
- 按需求公式处理病假、事假考勤扣款，工作部门可空且请假信息分组头不要求目标表存在
  同名字段，规则未确认的特殊假别继续停止；
- 上月完整工资表会遍历离职名单、备忘等分表和全部表区，已生效记录先进入确认流程，
  不再把分表已经证明的离职误报为目标月附件缺人；
- 转正统一按生效月份 1 日处理，不再要求转正天数或试用天数；
- 增加《工资表需求》11 项覆盖状态，区分自动公式、来源数据和人工确认边界；
- 劳务费与当前草案金额不同改为不阻断写入的中性复核提醒，未验证高税档仍停止；
- 已有人员发生工资或考勤变动时保留主表`实发合计`公式，`代发薪`改为内部引用；
  若没有可重算公式且资料未明确金额，仍停止整批写入；
- 同批已确认离职腾出的`工资核对表`和`代发薪`行可供缺行人员复用，但必须验证核对
  公式、身份证和来源工资卡号；
- 扩充无个人信息合成回归至 19 项，并继续保持零外链与完整工作簿事务门禁。

生成结果仍是待复核工资表，必须在桌面版 Excel 中完整重算并由财务人员确认。

## v1.1.1 — 2026-07-28

- 修复同批选择三类附件、其中仅一份加密时，正确输入密码后其余文件状态丢失的问题；
- 目标月份文件内只有唯一的紧邻上月工作表时允许读取，并明确显示月份提醒；
- 分开显示附件“已读取”和“通过核对”，人员差异会引导先确认人员变动；
- 目标月附件值即使与上月相同也按来源重新写入，不再用“数值有没有变化”判断沿用；
- 增加一次性项目重置、季度绩效与季度补贴资料门禁；
- 支持已证明格式的业务 Excel/Word 形成逐人预览，只有人员名单、没有金额时停止；
- 使用说明明确生成前资料核对与生成后 Excel/财务复核两道审核。

生成结果仍是待复核工资表，必须在桌面版 Excel 中完整重算并由财务人员确认。

## v1.1.0 — 2026-07-28

- 遇到受密码保护的 Office 工作簿时直接弹出密码框，不再要求先另存为无密码副本；
- 密码错误可以重试，取消后不导入该文件，不同文件可以使用不同密码；
- 密码只用于当次本地解密，不进入应用状态、浏览器存储、日志或导出文件；
- 增加标准加密与敏捷加密工作簿的无个人信息端到端回归；
- 修正附件预览中“来源匹配人数”和“应覆盖人数”混用导致的提示矛盾；
- 补充 officecrypto-tool 第三方许可，并继续保持纯本地、无网络运行。

生成结果仍是待复核工资表，必须在桌面版 Excel 中完整重算并由财务人员确认。

## v1.0.0 — 2026-07-28

- 提供普通月份递推和一月跨年两条独立生成路线；
- 一月路线实际要求并消费上一年度 12 个月工资表；
- 支持目标月份个税、社保、公积金及适用劳务费附件的逐项匹配；
- 导出前解析已确认来源并移除全部外链；
- 人员和工资变动按已验证结构同步完整工作簿；
- 提供无个人信息合成端到端测试和失败回滚门禁；
- 提供 Windows、macOS、桌面 Linux 通用使用包；
- 提供 README、`CODEX.md`、维护包和跨平台重新打包工具；
- 项目采用 Apache License 2.0。

生成结果仍是待复核工资表，必须在桌面版 Excel 中完整重算并由财务人员确认。
