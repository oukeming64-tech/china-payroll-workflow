# Codex 维护指南

本文件是领导电脑上的 Codex 或其他代码 Agent 的首要维护入口。

## 先确认任务边界

本工具的业务模型只有一个：

```text
上月已确认工资表
+ 目标月份各部门提供的数据和官方附件
+ 已确认规则
→ 目标月份待复核工资表
```

它不是根据历史数据自动猜出所有工资。考勤、绩效、提成、人员去留、个税、社保和公积金
等当月事实必须由对应来源提供；没有来源时应显示待输入或错误。页面核对是生成前的第一道
审核，导出的“待复核”工作簿还必须在桌面版 Excel 中完整重算并由财务进行第二道审核。

开始修改前，依次阅读：

1. `AGENTS.md`
2. `requirements/PRODUCT_SPEC.md`
3. `requirements/ACCEPTANCE.md`
4. 与目标功能直接相关的 `app/excel/`、`app/rules/`、`app/ui/` 和测试

## 新表单或格式变化时怎样修改

1. 只读检查文件名、工作表名、表头、已用区域、人员键和公式模式；
2. 优先要求空白模板或已脱敏样例，禁止把真实行级数据复制进源码、日志或测试；
3. 明确“来源字段 → 工资字段”的证据，不能凭字段名或常识猜公式；
4. Excel/OOXML 能力放入 `app/excel/`；
5. 业务识别和匹配放入独立的 `app/rules/` 模块；
6. 页面只在 `app/ui/` 编排，不复制业务计算；
7. 提供逐项文件、工作表、字段、人员、冲突和缺失预览；
8. 无法唯一匹配、来源月份错误、公式无缓存或完整工作簿无法同步时停止并回滚；
9. 为新格式增加无个人信息合成夹具，覆盖正常、缺失、冲突和回滚；已证明格式的 Word
   审批表先转成统一表格，不允许从只有人员名单的资料推算金额；
10. 同步产品规格、验收清单、README 和必要的使用说明。

人员匹配规则为：

```text
来源含身份证：只按身份证
来源不含身份证：仅使用已经逐人验证的稳定键或唯一姓名规则
```

身份证存在但未命中时禁止改用姓名兜底。姓名只能在来源没有身份证、规则已验证且唯一
无冲突时使用。新增、停用和工资变动必须同步所有经验证的相关工作表；任一张表无法
安全同步时整批停止。劳务费金额与当前草案不同只形成中性复核提醒，不阻断普通月度
写入；进入未验证高税档时仍停止。

读取上月完整工资表时必须遍历全部工作表和表格区段。离职名单、备忘等分表中带有明确
业务日期的记录要按生效月份形成待确认项；更早历史只作为证据，不重复执行。已确认的
既往离职才可以从目标月附件的在职覆盖范围中排除。

已有人员发生工资或考勤变动时，若主表`实发合计`是可重算内部公式，应保留该公式并让
`代发薪`金额内部引用它，不要求来源重复提供结果值。缺少结果公式且来源没有明确金额
时必须停止。辅助表缺行只能复用同批已确认离职腾出的行，并验证公式、身份和银行账号。

## 公式与外链

- 上月工资表提供结构、样式、内部公式和历史缓存；
- 普通月份将已确认累计接入下月对应累计字段；
- 是否沿用先看字段来源，不以数值是否变化判断；目标月份附件即使同值也要实际写入；
- 一次性绩效、提成、其他工资和扣款先重置，季度项目只按目标月份已确认资料录入；
- 一月必须实际读取上一年度 12 个月并验证年度结构与累计重置；
- 目标月份附件通过后才可把来源写为本地值；
- 转正统一按生效月份 1 日采用整月转正标准，不要求转正天数或试用天数；
- 导出前移除外部关系、外链部件和外部公式并复检为 0；
- 未解析外部公式、错误值或缺失缓存不得静默固定；
- 不硬编码真实人员差异、工资值、政策阈值或未经证明的例外。

## 代码门禁

- 依赖方向：`excel → rules → ui`；
- `app/rules/*.js` 不超过 400 行；
- `app/excel/*.js`、`app/ui/*.js` 不超过 500 行；
- 入口文件不超过 100 行；
- 新规则使用独立模块和纯函数，并登记脚本加载顺序；
- 工作簿解密只放在 `app/excel/`，密码提示只放在 `app/ui/`；密码不得进入持久状态、
  日志、测试数据或导出文件；
- 真实工资表、截图、导出和回放不得进入 Git。

## 必跑命令

```bash
npm install
npx playwright install chromium
npm run check:architecture
npm test
npm run build
npm run package:maintenance
```

如使用已安装的 Chrome 或 Edge，可设置 `PLAYWRIGHT_CHANNEL=chrome` 或
`PLAYWRIGHT_CHANNEL=msedge`。

交付前还要检查：

- 两种 ZIP 均能通过完整性和 SHA-256 校验；
- ZIP 内没有 Excel、CSV、Word、PDF、真实回放或 `.git`；
- 文本扫描没有身份证号、手机号、银行卡号候选、本机路径、密钥或内部工作簿名；
- 普通月、一月跨年与加密工作簿测试通过；
- 导出保留全部经验证工作表且外链为 0；
- README、使用说明、版本号和 Release 资产名称一致；
- Git 暂存区只包含本次有意图的源码、合成测试和文档。

## 误删后的恢复

项目永久恢复入口：

<https://github.com/oukeming64-tech/china-payroll-workflow>

- 日常使用包和维护包：进入 GitHub 的 **Releases**，下载最新版本；
- 完整源码：

```bash
git clone https://github.com/oukeming64-tech/china-payroll-workflow.git
cd china-payroll-workflow
npm install
npx playwright install chromium
```

不要从旧电脑复制 `node_modules`、真实工资表或历史导出到仓库。重新克隆后，用脱敏模板和
合成测试恢复开发环境即可。

## 可直接交给 Codex 的任务开场

> 请先完整阅读 AGENTS.md、CODEX.md、产品规格和验收清单。我要新增或调整一类工资
> 来源表。先只读分析表头、工作表结构、人员键和公式模式，禁止记录真实人员值；把识别
> 规则放入独立模块，补无个人信息合成测试。无法唯一匹配、字段缺失、月份不一致或完整
> 工作簿无法安全同步时必须停止。完成后运行架构检查、浏览器测试、两类发布构建和隐私
> 扫描，并同步 README 与验收清单。
