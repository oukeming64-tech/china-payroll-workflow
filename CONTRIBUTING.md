# 贡献指南

欢迎改进表单适配、匹配预览、错误门禁、工作簿兼容性和无个人信息测试。

## 隐私红线

任何提交、Issue、PR、日志、截图或测试夹具都不得包含：

- 真实姓名、身份证号、银行卡号、手机号；
- 真实工资、奖金、津贴、社保、个税或公积金数值；
- 真实工资表、部门附件、回放导出或工作簿指纹；
- 本机绝对路径、账号令牌、密码或密钥。

请使用空白模板、脱敏结构说明和合成人员编号。发现公开内容可能包含真实数据时，请先
停止传播并联系仓库所有者处理，不要在公开 Issue 中粘贴样本。

## 修改要求

- 保持 `excel → rules → ui` 依赖方向；
- 新表单识别使用独立规则模块；
- 没有来源证据时停止，不猜公式或业务数值；
- 补充无个人信息合成测试；
- 同步 README、`CODEX.md`、产品规格和验收清单；
- 运行 `npm run check:architecture`、`npm test`、`npm run build` 和
  `npm run package:maintenance`。

提交说明应写清变更、原因、影响、验证和仍需人工确认的边界。
